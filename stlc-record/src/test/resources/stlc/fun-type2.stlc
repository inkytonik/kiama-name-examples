// From https://github.com/MetaBorgCube/oopsla18-artifact/blob/master/lang.stlcrec.test/stlc.spt
let add = fun(x : num) { fun(y : num) { x + y } } in
let id  = fun(f : num -> num){ fun(x : num) { f x } } in
id (add 1) 2 
